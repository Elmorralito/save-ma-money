# Repository Codecov configuration — validate with:
#   curl --data-binary @codecov.yml https://codecov.io/validate
# Docs: https://docs.codecov.com/docs/codecov-yaml

codecov:
  branch: main
  require_ci_to_pass: true
  # CI uploads a single pytest-cov XML per commit; 12h default is sufficient.
  max_report_age: 12h

coverage:
  precision: 2
  round: down
  range: 70..95

  status:
    project:
      default:
        target: auto
        threshold: 1%
        if_ci_failed: error
        # Soft while PPT-040 switches measurement from test paths → modules/*/src
        # (expects a large rebaseline). Set informational: false after the new
        # source report is the Codecov base on main.
        informational: true
    patch:
      default:
        target: auto
        threshold: 0%
        if_ci_failed: error
        # Source packages measured via pytest --cov=modules/{model,api}/src (PPT-040).
        informational: false

comment:
  layout: "condensed_header, condensed_files, condensed_footer"
  behavior: default
  require_changes: false
  require_base: true
  require_head: true
  hide_project_coverage: false

flags:
  unittests:
    paths:
      - modules/
    carryforward: true

ignore:
  - "**/.venv/**"
  - "**/node_modules/**"
  - "docs/**"
  - "bin/**"
  - "docker/**"
  - ".github/**"
  - ".strata/**"
  - ".cursor/**"
  - ".agents/**"
  - "**/alembic/versions/**"
