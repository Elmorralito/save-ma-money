---
id: 20260713-02
type: task
status: open
priority: med
area: modules/api
created: 2026-07-13
---

**What:** PPT-040 ([#50](https://github.com/Elmorralito/save-ma-money/issues/50)) — close-out: live txn/movement suite,
source `--cov` for Codecov, optional secret-backed B1 workflow (`supabase-b1-smoke.yml`).

**Why:** Epic #42 needs `modules/api/tests` + B0 CI green; B1 pooler is optional ops (Auth-first pivot). QC uses
`AUTH_PROVIDER=local`; Supabase Auth smoke stays `make auth-smoke`.

**Done when:** B0 live routers covered (incl. txns/movements), Codecov measures `modules/*/src`,
`SUPABASE_B1_DATABASE_URL` runs B1 on `main`/`workflow_dispatch`, README/Make targets document local B1.
