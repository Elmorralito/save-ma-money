---
id: 20260713-02
type: task
status: open
priority: med
area: modules/api
created: 2026-07-13
---

**What:** PPT-040 ([#50](https://github.com/Elmorralito/save-ma-money/issues/50)) — close-out: live txn/movement suite,
source `--cov` for Codecov, B0 CI green. **Auth-first:** Supabase = users/Auth/tokens only (no Supabase PG gate).

**Why:** Epic #42 needs `modules/api/tests` + B0 Docker Postgres CI. Dual-target in the original #50 body meant
B0 + optional hosted PG; post-PPT-039 the epic waived Supabase-hosted Postgres. Staging Auth → `make auth-smoke`.

**Done when:** B0 live routers covered (incl. txns/movements), Codecov measures `modules/*/src`, QC
`AUTH_PROVIDER=local`, README documents Auth smoke (not pooler DB) as the Supabase validation path;
`codecov/patch` green on PR [#92](https://github.com/Elmorralito/save-ma-money/pull/92).

**Progress:** Unit tests for `LinkedEntitiesService.get` (`test_linked_entities_get.py`). Codecov still at
~85.7% patch vs `target: auto` (~97.7% base) until the isolation fix lands: clone fresh unloaded
`LinkedEntity` instances per test — class-level `__links__` is mutated in place by `load_link_services`,
so suite order hid the `not isinstance(...): continue` branch. Resolve/fallback collapsed to a ternary
(no bare `else:` miss). Uncommitted on `test/PPT-050` pending this strata pair.
