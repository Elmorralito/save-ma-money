# Product — save-ma-money

PRDs and product requirements, one per `<slug>.md`: what should exist, for whom, and what "done" means. The _why-build-it_ layer — how it works belongs in `../architecture/`, why it was decided this way in `../decisions/`.
