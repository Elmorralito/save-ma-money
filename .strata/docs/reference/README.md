# Reference — save-ma-money

Stable facts you look up: paths, schemas, API surfaces, naming conventions, external system quirks. One topic per `<slug>.md`. The discriminator: a _fact_ lives here; a _procedure_ (steps you execute) lives in `../ops/`; a _rule_ that changes agent behavior lives in `.strata/memory/learnings/`.

Rule of thumb: the second time you look something up, write it down here.
